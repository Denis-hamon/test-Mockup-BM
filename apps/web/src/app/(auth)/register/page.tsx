import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Creer un compte - LegalConnect",
  description: "Creez votre compte LegalConnect pour acceder a votre espace juridique securise.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
