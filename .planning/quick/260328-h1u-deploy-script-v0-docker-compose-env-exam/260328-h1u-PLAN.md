---
phase: quick
plan: 260328-h1u
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - apps/web/Dockerfile
  - scripts/seed.ts
  - README-DEPLOY.md
autonomous: true
requirements: [DEPLOY-V0]
must_haves:
  truths:
    - "docker compose up -d starts PostgreSQL 16, Valkey 9, and Next.js app"
    - ".env.example lists every required env var with OVH AI Endpoints config"
    - "seed script creates 1 avocat, 1 client, 3 intake templates, 1 dossier with AI case summary"
    - "README-DEPLOY.md gives a working 5-step deploy guide"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Multi-service Docker Compose for PostgreSQL, Valkey, Next.js app"
    - path: ".env.example"
      provides: "Complete env var reference with OVH AI Endpoints"
    - path: "apps/web/Dockerfile"
      provides: "Multi-stage Next.js production build"
    - path: "scripts/seed.ts"
      provides: "DB migration + seed data"
    - path: "README-DEPLOY.md"
      provides: "5-step deploy instructions"
  key_links:
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "env_file directive"
      pattern: "env_file.*\\.env"
    - from: "scripts/seed.ts"
      to: "apps/web/src/lib/db/schema"
      via: "Drizzle schema imports"
      pattern: "import.*schema"
---

<objective>
Create a v0 deployment kit for LegalConnect: Docker Compose infrastructure, complete env config with OVH AI Endpoints, seed script with test data, and a 5-step deploy guide.

Purpose: Enable first deployment on ubuntu@141.95.99.214 with all services running and testable data.
Output: docker-compose.yml, .env.example (updated), apps/web/Dockerfile, scripts/seed.ts, README-DEPLOY.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/web/src/lib/db/schema/index.ts
@apps/web/src/lib/db/schema/auth.ts
@apps/web/src/lib/db/schema/intake.ts
@apps/web/src/lib/db/schema/intake-templates.ts
@apps/web/src/lib/db/schema/case-intelligence.ts
@apps/web/src/lib/db/schema/lawyer.ts
@apps/web/src/lib/db/schema/messaging.ts
@apps/web/src/lib/db/schema/encryption.ts
@apps/web/src/lib/db/schema/consent.ts
@apps/web/src/lib/db/schema/appointments.ts
@apps/web/src/lib/db/seed/intake-templates.ts
@apps/web/drizzle.config.ts
@apps/web/next.config.ts
@apps/web/package.json
@package.json
@pnpm-workspace.yaml
@turbo.json
@packages/ai/src/config.ts
@packages/ai/src/providers/openai.ts

<interfaces>
<!-- Existing .env.example at root (basic): DATABASE_URL, NEXTAUTH_URL, NEXTAUTH_SECRET, RESEND_API_KEY, EMAIL_FROM -->
<!-- Existing .env.example at apps/web (more complete): DATABASE_URL, AUTH_SECRET, RESEND_API_KEY, OVH_S3_* -->
<!-- AI config reads: AI_PROVIDER, AI_MODEL, AI_TEMPERATURE, AI_MAX_TOKENS, OPENAI_API_KEY -->
<!-- OpenAI SDK supports OPENAI_BASE_URL for OVH AI Endpoints compatibility -->
<!-- Drizzle config: schema at ./src/lib/db/schema, out at ./drizzle, dialect postgresql -->
<!-- Schema tables: users, accounts, sessions, verification_tokens, password_reset_tokens, email_verification_tokens, encryption_keys, consents, intake_submissions, intake_documents, extraction_results, ai_follow_ups, case_summaries, case_timelines, qualification_scores, lawyer_profiles, lawyer_notes, conversations, messages, appointments, reminder_logs, intake_templates, intake_template_snapshots -->
<!-- Seed templates: familyTemplate, laborTemplate, criminalTemplate (from apps/web/src/lib/db/seed/intake-templates.ts) -->
<!-- User roles: "avocat" | "client" -->
<!-- intakeSubmissions.status: "draft" | "submitted" | "en_cours" | "termine" | "archive" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose + Dockerfile + .env.example</name>
  <files>docker-compose.yml, apps/web/Dockerfile, .env.example</files>
  <action>
**docker-compose.yml** at monorepo root with 3 services:

1. `db` — PostgreSQL 16 (image: `postgres:16-alpine`)
   - Port 5432 exposed
   - Volume `pgdata:/var/lib/postgresql/data`
   - Environment: POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB from .env
   - Healthcheck: `pg_isready -U $POSTGRES_USER`

2. `valkey` — Valkey 9 (image: `valkey/valkey:9-alpine`)
   - Port 6379 exposed
   - Volume `valkeydata:/data`
   - Healthcheck: `valkey-cli ping`

3. `web` — Next.js app
   - Build from `apps/web/Dockerfile` with context `.` (monorepo root needed for pnpm workspace)
   - Port 3000 exposed
   - `env_file: .env`
   - depends_on db (healthy) and valkey (healthy)
   - Environment override: DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

Volumes: pgdata, valkeydata (named volumes)

**apps/web/Dockerfile** — Multi-stage build:
- Stage 1 `base`: node:22-alpine, install pnpm globally, set workdir /app
- Stage 2 `deps`: copy package.json, pnpm-lock.yaml, pnpm-workspace.yaml, turbo.json, all packages/*/package.json, apps/web/package.json, apps/widget/package.json. Run `pnpm install --frozen-lockfile`
- Stage 3 `builder`: copy all source, run `pnpm --filter @legalconnect/web build`. Set NEXT_TELEMETRY_DISABLED=1
- Stage 4 `runner`: node:22-alpine, create non-root user `nextjs`, copy .next/standalone, .next/static, public from builder. Expose 3000, CMD ["node", "apps/web/server.js"]
- Note in comment: Next.js must have `output: "standalone"` in next.config.ts for this to work — the seed script or README should mention adding it.

**Update .env.example** at monorepo root — REPLACE the existing file with comprehensive version:

```
# ============================================
# LegalConnect — Environment Variables
# ============================================

# --- PostgreSQL ---
POSTGRES_USER=legalconnect
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=legalconnect
DATABASE_URL=postgresql://legalconnect:change_me_in_production@localhost:5432/legalconnect

# --- Auth.js ---
AUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# --- AI (OVHcloud AI Endpoints — OpenAI-compatible) ---
# Catalog: https://www.ovhcloud.com/en/public-cloud/ai-endpoints/catalog/
AI_PROVIDER=openai
AI_MODEL=Qwen/Qwen2.5-72B-Instruct
OPENAI_API_KEY=your-ovh-ai-endpoints-token
OPENAI_BASE_URL=https://qwen2.5-72b-instruct.endpoints.kepler.ai.cloud.ovh.net/api/openai_compat/v1

# --- OVHcloud Object Storage (S3-compatible) ---
OVH_S3_ENDPOINT=https://s3.gra.io.cloud.ovh.net
OVH_S3_REGION=gra
OVH_S3_ACCESS_KEY=
OVH_S3_SECRET_KEY=
OVH_S3_BUCKET=legalconnect-documents

# --- Email (Resend) ---
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=LegalConnect <noreply@legalconnect.example.com>

# --- Valkey (Redis-compatible) ---
VALKEY_URL=redis://localhost:6379

# --- Cron ---
CRON_SECRET=generate-a-random-secret

# --- App ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

Comments should explain each section. OVH AI Endpoints section should note that OPENAI_BASE_URL is the model-specific endpoint URL from the OVH catalog, and AI_PROVIDER=openai because OVH endpoints are OpenAI-compatible.
  </action>
  <verify>
    <automated>cat docker-compose.yml && echo "---" && cat apps/web/Dockerfile && echo "---" && cat .env.example | head -40</automated>
  </verify>
  <done>docker-compose.yml defines 3 services (db, valkey, web) with healthchecks. Dockerfile has 4-stage build. .env.example has all vars including OVH AI Endpoints config.</done>
</task>

<task type="auto">
  <name>Task 2: Seed script + README-DEPLOY.md</name>
  <files>scripts/seed.ts, README-DEPLOY.md</files>
  <action>
**scripts/seed.ts** — TypeScript seed script using Drizzle ORM directly.

At top: import from `drizzle-orm/postgres-js` and `postgres` (add postgres to root devDeps if needed). Import all schemas from `../apps/web/src/lib/db/schema`. Import `seedTemplates` from `../apps/web/src/lib/db/seed/intake-templates`.

The script should:

1. Connect to DB using DATABASE_URL from env
2. Run `drizzle-kit push` first via child_process exec (command: `cd apps/web && npx drizzle-kit push --force`) to apply schema
3. Check if seed data already exists (select count from users) — skip if already seeded
4. Create test avocat user:
   - email: `avocat@test.legalconnect.fr`
   - name: `Me Sophie Martin`
   - role: `avocat`
   - passwordHash: bcrypt hash of `Test1234!`
   - emailVerified: now
5. Create avocat's lawyer profile:
   - firmName: `Cabinet Martin & Associes`
   - specialties: `["famille","travail","penal"]`
   - phone: `+33 1 23 45 67 89`
6. Create test client user:
   - email: `client@test.legalconnect.fr`
   - name: `Jean Dupont`
   - role: `client`
   - passwordHash: bcrypt hash of `Test1234!`
   - emailVerified: now
7. Create 3 intake templates (one per specialty) linked to the avocat user, using the `seedTemplates` data:
   - For each template: insert into `intakeTemplates` with lawyerId = avocat.id, specialty from template, schema = the template object as JSON, isActive = 1
   - Generate a slug: `me-sophie-martin-{specialty}`
8. Create 1 intake submission (test dossier):
   - userId: client.id
   - problemType: `famille`
   - description: `Mon conjoint et moi souhaitons divorcer a l'amiable. Nous avons 2 enfants (6 et 9 ans) et un bien immobilier commun. Nous sommes d'accord sur la garde alternee mais pas sur la repartition du bien.`
   - fullName: `Jean Dupont`
   - phone: `+33 6 12 34 56 78`
   - status: `submitted`
   - templateId: famille template id
9. Create 1 case summary (AI intelligence):
   - submissionId: the submission above
   - summary: `Dossier de divorce amiable avec deux points de negociation : la garde des enfants (accord sur alternance) et la repartition d'un bien immobilier commun. Situation relativement favorable a une resolution rapide par consentement mutuel.`
   - keyFacts: JSON.stringify(["Divorce amiable", "2 enfants (6 et 9 ans)", "Bien immobilier commun", "Accord garde alternee", "Desaccord repartition bien"])
   - legalDomain: `famille`
   - parties: JSON.stringify([{name: "Jean Dupont", role: "Demandeur"}, {name: "Conjoint(e) Dupont", role: "Partie adverse"}])
   - urgencyAssessment: `low`
   - aiModel: `seed-data`
   - aiTokensUsed: 0
10. Close DB connection, log success

Use `tsx` to run: `npx tsx scripts/seed.ts`

**README-DEPLOY.md** at monorepo root:

Title: "LegalConnect - Guide de deploiement v0"

Section 1 - Prerequis: Docker, Docker Compose v2+, Node.js 22+ (for seed only), pnpm 10+, acces au serveur ubuntu@141.95.99.214

Section 2 - Configuration:
- `cp .env.example .env`
- Edit .env: set POSTGRES_PASSWORD, AUTH_SECRET (openssl rand -base64 32), OVH AI token, S3 credentials, CRON_SECRET
- Explain OVH AI Endpoints: go to catalog, get token, use the model endpoint URL for OPENAI_BASE_URL

Section 3 - Add standalone output:
- Note: add `output: "standalone"` to nextConfig in `apps/web/next.config.ts` before building
- Show the exact code change

Section 4 - Demarrage:
```bash
docker compose up -d
```
- Wait for healthchecks: `docker compose ps` (all should show "healthy")
- Check logs: `docker compose logs web`

Section 5 - Seed (donnees de test):
```bash
pnpm install
npx tsx scripts/seed.ts
```
- Lists the 2 test accounts (avocat@test.legalconnect.fr / Test1234!, client@test.legalconnect.fr / Test1234!)
- Lists the 3 templates created

Section 6 - Verification:
- Open http://141.95.99.214:3000 (or localhost:3000 if local)
- Login as avocat, check dashboard shows 1 dossier
- Login as client, check dossier visible

All in French. Clean markdown, no fluff.
  </action>
  <verify>
    <automated>cat scripts/seed.ts | head -20 && echo "---" && cat README-DEPLOY.md | head -30</automated>
  </verify>
  <done>scripts/seed.ts creates complete test data (2 users, 3 templates, 1 dossier, 1 AI summary). README-DEPLOY.md provides 5-step guide in French. npx tsx scripts/seed.ts runs without error against a running PostgreSQL.</done>
</task>

</tasks>

<verification>
- `docker compose config` validates docker-compose.yml syntax
- `cat .env.example` shows all env vars with OVH AI Endpoints config
- `cat apps/web/Dockerfile` shows 4-stage multi-stage build
- `cat scripts/seed.ts` shows seed logic with all required data
- `cat README-DEPLOY.md` shows 5-step deploy guide in French
</verification>

<success_criteria>
- docker-compose.yml defines PostgreSQL 16, Valkey 9, and Next.js app services with healthchecks
- .env.example has every env var needed including OVH AI Endpoints (AI_PROVIDER=openai, OPENAI_BASE_URL, OPENAI_API_KEY)
- Dockerfile uses multi-stage build (base, deps, builder, runner)
- Seed script creates: 1 avocat (Me Sophie Martin), 1 client (Jean Dupont), 3 intake templates, 1 submitted dossier, 1 case summary
- README-DEPLOY.md has 5 clear steps in French with test account credentials
</success_criteria>

<output>
After completion, create `.planning/quick/260328-h1u-deploy-script-v0-docker-compose-env-exam/260328-h1u-SUMMARY.md`
</output>
