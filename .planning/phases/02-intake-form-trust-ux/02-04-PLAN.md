---
phase: 02-intake-form-trust-ux
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/intake/intake-stepper.tsx
autonomous: true
gap_closure: true
requirements:
  - INTK-01
  - INTK-03
  - INTK-04
  - SECU-03

must_haves:
  truths:
    - "Client completes a multi-step form with all 4 step components rendered (StepProblemType, StepDescription, StepDocuments, StepContact)"
    - "Form state persists to localStorage via useIntakeForm hook and restores on revisit"
    - "TrustBanner is visible on step 0 (first step)"
    - "Submitting the form calls submitIntake server action with validated data"
    - "Step navigation validates current step fields before advancing"
  artifacts:
    - path: "apps/web/src/components/intake/intake-stepper.tsx"
      provides: "Fully wired stepper connecting all orphaned components"
      min_lines: 80
      contains: "useIntakeForm"
  key_links:
    - from: "apps/web/src/components/intake/intake-stepper.tsx"
      to: "apps/web/src/hooks/use-intake-form.ts"
      via: "useIntakeForm hook import"
      pattern: "import.*useIntakeForm.*use-intake-form"
    - from: "apps/web/src/components/intake/intake-stepper.tsx"
      to: "apps/web/src/components/intake/step-problem-type.tsx"
      via: "StepProblemType component render"
      pattern: "<StepProblemType"
    - from: "apps/web/src/components/intake/intake-stepper.tsx"
      to: "apps/web/src/components/intake/step-description.tsx"
      via: "StepDescription component render"
      pattern: "<StepDescription"
    - from: "apps/web/src/components/intake/intake-stepper.tsx"
      to: "apps/web/src/components/intake/step-contact.tsx"
      via: "StepContact component render"
      pattern: "<StepContact"
    - from: "apps/web/src/components/intake/intake-stepper.tsx"
      to: "apps/web/src/components/trust/trust-banner.tsx"
      via: "TrustBanner render on step 0"
      pattern: "<TrustBanner"
    - from: "apps/web/src/components/intake/intake-stepper.tsx"
      to: "apps/web/src/server/actions/intake.actions.ts"
      via: "submitIntake call on final step"
      pattern: "submitIntake"
---

<objective>
Rewrite intake-stepper.tsx to wire all orphaned Phase 2 components that were disconnected during a parallel worktree merge.

Purpose: Plans 02-02 and 02-03 both modified intake-stepper.tsx. When the worktrees merged, 02-03's minimal stub (with only StepDocuments at step 2) overwrote 02-02's full implementation. All step components, the useIntakeForm hook, TrustBanner, and the submit action exist and are complete on disk -- they are simply not imported or rendered. This plan rewrites the single file to reconnect everything.

Output: A fully functional intake-stepper.tsx that renders all 4 steps, uses useIntakeForm for localStorage persistence + validation, shows TrustBanner on step 0, shows draft restoration alert, uses Progress + Badge for step indicators, uses useTranslations('intake') for all text, and wires form submission to submitIntake server action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intake-form-trust-ux/02-VERIFICATION.md

<interfaces>
<!-- All components below exist, are complete, and must be imported into intake-stepper.tsx -->

From apps/web/src/hooks/use-intake-form.ts:
```typescript
export function useIntakeForm(): {
  form: UseFormReturn<IntakeFormData>;
  currentStep: number;
  setCurrentStep: (step: number | ((prev: number) => number)) => void;
  validateStep: (stepIndex: number) => Promise<boolean>;
  nextStep: () => Promise<boolean>;
  prevStep: () => void;
  hasDraft: boolean;
  storageFull: boolean;
  clearDraft: () => void;
};
```

From apps/web/src/components/intake/step-problem-type.tsx:
```typescript
export function StepProblemType({ form }: { form: UseFormReturn<IntakeFormData> }): JSX.Element;
```

From apps/web/src/components/intake/step-description.tsx:
```typescript
export function StepDescription({ form }: { form: UseFormReturn<IntakeFormData> }): JSX.Element;
```

From apps/web/src/components/intake/step-documents.tsx:
```typescript
export function StepDocuments({ form }: { form: UseFormReturn<IntakeFormData> }): JSX.Element;
```

From apps/web/src/components/intake/step-contact.tsx:
```typescript
export function StepContact({ form }: { form: UseFormReturn<IntakeFormData> }): JSX.Element;
```

From apps/web/src/components/trust/trust-banner.tsx:
```typescript
export function TrustBanner({ className }: { className?: string }): JSX.Element;
```

From apps/web/src/server/actions/intake.actions.ts:
```typescript
export async function submitIntake(data: unknown): Promise<{ success: boolean; id?: string; error?: string }>;
```

Available shadcn/ui components in apps/web/src/components/ui/:
- alert.tsx (Alert, AlertDescription, AlertTitle)
- badge.tsx (Badge)
- button.tsx (Button)
- progress.tsx (Progress)

From next-intl:
```typescript
import { useTranslations } from "next-intl";
// Usage: const t = useTranslations("intake");
// Keys: t("steps.problemType"), t("steps.description"), t("steps.documents"), t("steps.contact")
// t("navigation.previous"), t("navigation.next"), t("navigation.submit")
// t("draft.title"), t("draft.description"), t("draft.restore"), t("draft.discard")
// t("submit.submitting"), t("submit.success"), t("submit.error")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite intake-stepper.tsx to wire all orphaned components</name>
  <files>apps/web/src/components/intake/intake-stepper.tsx</files>
  <read_first>
    - apps/web/src/components/intake/intake-stepper.tsx (current stub to replace)
    - apps/web/src/hooks/use-intake-form.ts (hook API: form, currentStep, nextStep, prevStep, hasDraft, clearDraft, storageFull)
    - apps/web/src/components/intake/step-problem-type.tsx (props: { form })
    - apps/web/src/components/intake/step-description.tsx (props: { form })
    - apps/web/src/components/intake/step-documents.tsx (props: { form })
    - apps/web/src/components/intake/step-contact.tsx (props: { form })
    - apps/web/src/components/trust/trust-banner.tsx (props: { className? })
    - apps/web/src/server/actions/intake.actions.ts (submitIntake(data) => { success, id?, error? })
    - apps/web/messages/fr.json (translation keys under "intake")
  </read_first>
  <action>
Rewrite `apps/web/src/components/intake/intake-stepper.tsx` completely. The current file is a minimal stub. Replace it with a full implementation that does ALL of the following:

**Imports (replace all current imports):**
- `useIntakeForm` from `@/hooks/use-intake-form` (replaces inline useForm + useState)
- `StepProblemType` from `@/components/intake/step-problem-type`
- `StepDescription` from `@/components/intake/step-description`
- `StepDocuments` from `@/components/intake/step-documents`
- `StepContact` from `@/components/intake/step-contact`
- `TrustBanner` from `@/components/trust/trust-banner`
- `submitIntake` from `@/server/actions/intake.actions`
- `useTranslations` from `next-intl`
- `useState` from `react` (for isSubmitting, submitResult states)
- shadcn/ui: `Button` from `@/components/ui/button`, `Progress` from `@/components/ui/progress`, `Badge` from `@/components/ui/badge`, `Alert`+`AlertDescription`+`AlertTitle` from `@/components/ui/alert`
- `cn` from `@/lib/utils`
- `AlertCircle`, `CheckCircle2`, `Loader2` from `lucide-react`

**Hook usage:**
- Call `useIntakeForm()` to get `{ form, currentStep, nextStep, prevStep, hasDraft, clearDraft, storageFull }`
- Call `useTranslations("intake")` to get `t`
- Add `const [isSubmitting, setIsSubmitting] = useState(false)`
- Add `const [submitResult, setSubmitResult] = useState<{ success: boolean; id?: string; error?: string } | null>(null)`

**Step labels array:**
```typescript
const STEPS = [
  { key: "problemType" },
  { key: "description" },
  { key: "documents" },
  { key: "contact" },
] as const;
```

**Progress bar (per D-01):**
- Render `<Progress value={((currentStep + 1) / STEPS.length) * 100} className="h-2" />` above the step indicator nav
- Step indicator nav uses numbered circles with Badge for current/completed/upcoming states:
  - Current step: `<Badge variant="default">{index + 1}</Badge>` with step label text
  - Completed step (index < currentStep): `<Badge variant="secondary"><CheckCircle2 className="size-3" /></Badge>` with muted label
  - Future step: `<Badge variant="outline">{index + 1}</Badge>` with muted label
  - Horizontal separator `<div className="mx-2 h-px flex-1 bg-border" />` between steps
  - Use `t(\`steps.${step.key}\`)` for step labels instead of hardcoded French

**Draft restoration alert (per D-03):**
- If `hasDraft` is true AND `currentStep === 0`, show an Alert below the progress bar:
  ```tsx
  <Alert>
    <AlertCircle className="size-4" />
    <AlertTitle>{t("draft.title")}</AlertTitle>
    <AlertDescription className="flex items-center gap-4">
      {t("draft.description")}
      <Button variant="outline" size="sm" onClick={clearDraft}>{t("draft.discard")}</Button>
    </AlertDescription>
  </Alert>
  ```

**Storage full warning:**
- If `storageFull` is true, show a small warning text: `<p className="text-sm text-amber-600">{t("draft.storageFull")}</p>`

**TrustBanner (per D-08):**
- Render `<TrustBanner className="mb-4" />` on step 0 ONLY, before the step content

**Step content rendering:**
- `currentStep === 0`: `<StepProblemType form={form} />`
- `currentStep === 1`: `<StepDescription form={form} />`
- `currentStep === 2`: `<StepDocuments form={form} />`
- `currentStep === 3`: `<StepContact form={form} />`

**Navigation buttons:**
- Previous button: `onClick={prevStep}`, `disabled={currentStep === 0 || isSubmitting}`, text `t("navigation.previous")`, variant="outline"
- Next button (steps 0-2): `onClick={nextStep}`, text `t("navigation.next")`
- Submit button (step 3): `onClick={handleSubmit}`, text `isSubmitting ? t("submit.submitting") : t("navigation.submit")`, `disabled={isSubmitting}`
- If isSubmitting, show `<Loader2 className="mr-2 size-4 animate-spin" />` inside submit button

**handleSubmit function:**
```typescript
async function handleSubmit() {
  const valid = await form.trigger();
  if (!valid) return;
  setIsSubmitting(true);
  try {
    const result = await submitIntake(form.getValues());
    setSubmitResult(result);
    if (result.success) {
      // Clear draft on successful submission
      clearDraft();
    }
  } catch {
    setSubmitResult({ success: false, error: "submission_failed" });
  } finally {
    setIsSubmitting(false);
  }
}
```

**Submit result display:**
- If `submitResult?.success`: render a success Alert with CheckCircle2 icon and `t("submit.success")` message, hide the form
- If `submitResult?.error`: render an error Alert with AlertCircle icon and `t("submit.error")` message above navigation

**Key constraints:**
- Keep `"use client"` directive at top
- Do NOT create a new useForm -- use the one from useIntakeForm()
- Do NOT hardcode any French text -- use `t()` for everything
- Use `cn()` for conditional classNames
- All step components receive `form` prop (UseFormReturn<IntakeFormData>)
  </action>
  <verify>
    <automated>cd /Users/dhamon && pnpm exec tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - intake-stepper.tsx contains `import { useIntakeForm }` from `@/hooks/use-intake-form`
    - intake-stepper.tsx contains `import { StepProblemType }` from `@/components/intake/step-problem-type`
    - intake-stepper.tsx contains `import { StepDescription }` from `@/components/intake/step-description`
    - intake-stepper.tsx contains `import { StepContact }` from `@/components/intake/step-contact`
    - intake-stepper.tsx contains `import { TrustBanner }` from `@/components/trust/trust-banner`
    - intake-stepper.tsx contains `import { submitIntake }` from `@/server/actions/intake.actions`
    - intake-stepper.tsx contains `import { useTranslations }` from `next-intl`
    - intake-stepper.tsx contains `<Progress` component usage
    - intake-stepper.tsx contains `<Badge` component usage
    - intake-stepper.tsx renders `<StepProblemType form={form}` when currentStep === 0
    - intake-stepper.tsx renders `<StepDescription form={form}` when currentStep === 1
    - intake-stepper.tsx renders `<StepDocuments form={form}` when currentStep === 2
    - intake-stepper.tsx renders `<StepContact form={form}` when currentStep === 3
    - intake-stepper.tsx renders `<TrustBanner` only when currentStep === 0
    - intake-stepper.tsx calls `submitIntake` in handleSubmit
    - intake-stepper.tsx does NOT contain `useForm(` (uses useIntakeForm instead)
    - intake-stepper.tsx does NOT contain hardcoded step label strings like "Probleme juridique"
    - TypeScript compilation passes with no errors in intake-stepper.tsx
  </acceptance_criteria>
  <done>
    intake-stepper.tsx is a fully wired stepper that imports and renders all 4 step components via useIntakeForm hook, shows Progress bar and Badge step indicators, renders TrustBanner on step 0, shows draft restoration alert when hasDraft is true, validates steps before advancing via nextStep(), and calls submitIntake server action on final step submission. No placeholder text remains. All text uses useTranslations('intake').
  </done>
</task>

</tasks>

<verification>
After task completion, verify all gaps from 02-VERIFICATION.md are closed:

1. **Multi-step form gap (FAILED -> should be VERIFIED):**
   - `grep -c "StepProblemType\|StepDescription\|StepContact\|StepDocuments" apps/web/src/components/intake/intake-stepper.tsx` returns 4+ (all step components imported/rendered)
   - `grep -c "useIntakeForm" apps/web/src/components/intake/intake-stepper.tsx` returns 1+ (hook used instead of inline useForm)
   - `grep -c "submitIntake" apps/web/src/components/intake/intake-stepper.tsx` returns 1+ (server action wired)

2. **Trust indicators gap (PARTIAL -> should be VERIFIED):**
   - `grep -c "TrustBanner" apps/web/src/components/intake/intake-stepper.tsx` returns 1+ (TrustBanner rendered on step 0)

3. **No orphaned components remain:**
   - useIntakeForm is imported by intake-stepper.tsx
   - StepProblemType, StepDescription, StepContact are all imported by intake-stepper.tsx
   - TrustBanner is imported by intake-stepper.tsx
   - submitIntake is imported by intake-stepper.tsx
</verification>

<success_criteria>
- All 4 step components render in intake-stepper.tsx (no placeholder text divs)
- useIntakeForm hook provides form state with localStorage persistence
- TrustBanner visible on step 0
- Progress bar and Badge step indicators present
- Draft restoration alert shown when hasDraft is true
- Step validation fires before advancing (nextStep validates current step)
- Submit button on step 3 calls submitIntake server action
- All text uses useTranslations('intake') -- zero hardcoded French strings
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake-form-trust-ux/02-04-SUMMARY.md`
</output>
