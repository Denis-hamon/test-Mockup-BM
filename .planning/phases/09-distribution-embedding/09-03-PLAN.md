---
phase: 09-distribution-embedding
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/app/cabinet-[slug]/page.tsx
  - apps/web/src/app/cabinet-[slug]/layout.tsx
  - apps/web/src/app/cabinet-[slug]/loading.tsx
  - apps/web/src/app/cabinet-[slug]/opengraph-image.tsx
  - apps/web/src/components/landing/lawyer-hero.tsx
  - apps/web/src/components/landing/specialty-card.tsx
  - apps/web/src/app/(app)/settings/cabinet/integration/page.tsx
  - apps/web/src/components/integration/snippet-copy-block.tsx
  - apps/web/src/components/integration/widget-preview.tsx
  - apps/web/src/lib/db/schema/lawyer.ts
autonomous: true
requirements: [DIST-02]

must_haves:
  truths:
    - "Visiting /cabinet-dupont-avocats renders a landing page with the lawyer's name, specialties, and intake form"
    - "The landing page has correct OG meta tags (title, description) generated from lawyer profile"
    - "The lawyer sees a ready-to-copy script snippet in /settings/cabinet/integration"
    - "The hosted page link is copyable from the integration settings"
    - "A non-existent slug shows a 404 error page"
  artifacts:
    - path: "apps/web/src/app/cabinet-[slug]/page.tsx"
      provides: "Hosted lawyer landing page with hero + specialties + intake form"
      contains: "generateMetadata"
    - path: "apps/web/src/app/cabinet-[slug]/opengraph-image.tsx"
      provides: "Dynamic OG image with lawyer name and specialties"
      contains: "ImageResponse"
    - path: "apps/web/src/app/(app)/settings/cabinet/integration/page.tsx"
      provides: "Integration settings with script snippet and hosted page link"
      contains: "widget.js"
    - path: "apps/web/src/components/integration/snippet-copy-block.tsx"
      provides: "Copyable code block component"
      contains: "clipboard"
    - path: "apps/web/src/components/landing/lawyer-hero.tsx"
      provides: "Hero section with lawyer photo, name, specialties, CTA"
      contains: "Commencer ma demande"
  key_links:
    - from: "apps/web/src/app/cabinet-[slug]/page.tsx"
      to: "getTemplateBySlug"
      via: "server action call"
      pattern: "getTemplateBySlug"
    - from: "apps/web/src/app/cabinet-[slug]/page.tsx"
      to: "DynamicStepper"
      via: "component import"
      pattern: "DynamicStepper"
    - from: "apps/web/src/app/(app)/settings/cabinet/integration/page.tsx"
      to: "getLawyerProfile"
      via: "server action call"
      pattern: "getLawyerProfile"
---

<objective>
Create hosted lawyer landing pages at /cabinet-[slug] with hero section, specialty cards, and embedded intake form. Add integration settings page at /settings/cabinet/integration with copyable widget snippet and hosted page link.

Purpose: DIST-02 -- each lawyer gets a standalone public page as entry point for client intake, plus self-service tools to embed the widget on their own site (per D-04, D-05, D-07).
Output: /cabinet-[slug] route with dynamic OG images, /settings/cabinet/integration page with snippet + preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-distribution-embedding/09-CONTEXT.md
@.planning/phases/09-distribution-embedding/09-RESEARCH.md
@.planning/phases/09-distribution-embedding/09-UI-SPEC.md
@.planning/phases/08-intake-templates-customization/08-03-SUMMARY.md
@.planning/phases/09-distribution-embedding/09-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from codebase and Plan 01 -->

From apps/web/src/server/actions/template.actions.ts:
- getTemplateBySlug(slug: string) -- returns template with steps, questions, branding (logoUrl, accentColor, welcomeText)

From apps/web/src/server/actions/lawyer-settings.actions.ts:
- getLawyerProfile() -- returns { firmName, phone, specialties, notifyNewCase, ... }

From apps/web/src/components/intake/dynamic-stepper.tsx:
- DynamicStepper component -- reuse directly on hosted page (same as /intake/[slug])

From apps/web/src/components/intake/cobranding-footer.tsx:
- CobrandingFooter -- reuse on hosted page

From apps/web/src/app/intake/[slug]/page.tsx:
- Reference for standalone intake page pattern with CSS variables

From apps/web/src/lib/db/schema/lawyer.ts:
- lawyerProfiles: firmName, phone, specialties (JSON string[])
- Need to add: description (text, optional), photoUrl (text, optional) for hosted landing page

From Plan 01 (09-01):
- /api/widget/route.ts serves widget.js from same domain
- Widget configured via data-slug, data-color attributes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hosted lawyer landing page /cabinet-[slug]</name>
  <files>
    apps/web/src/app/cabinet-[slug]/page.tsx,
    apps/web/src/app/cabinet-[slug]/layout.tsx,
    apps/web/src/app/cabinet-[slug]/loading.tsx,
    apps/web/src/app/cabinet-[slug]/opengraph-image.tsx,
    apps/web/src/components/landing/lawyer-hero.tsx,
    apps/web/src/components/landing/specialty-card.tsx,
    apps/web/src/lib/db/schema/lawyer.ts
  </files>
  <read_first>
    apps/web/src/app/intake/[slug]/page.tsx,
    apps/web/src/app/intake/[slug]/layout.tsx,
    apps/web/src/app/intake/[slug]/loading.tsx,
    apps/web/src/components/intake/dynamic-stepper.tsx,
    apps/web/src/components/intake/cobranding-footer.tsx,
    apps/web/src/server/actions/template.actions.ts,
    apps/web/src/server/actions/lawyer-settings.actions.ts,
    apps/web/src/lib/db/schema/lawyer.ts,
    apps/web/src/lib/db/schema/intake-templates.ts,
    apps/web/src/lib/db/index.ts,
    apps/web/src/app/layout.tsx
  </read_first>
  <action>
    **1. Schema migration -- add description and photoUrl to lawyerProfiles:**
    - Add `description: text("description")` (nullable) to lawyerProfiles table.
    - Add `photoUrl: text("photo_url")` (nullable) to lawyerProfiles table.
    - Run `pnpm drizzle-kit push` to apply migration.

    **2. Create /cabinet-[slug] route (per D-04):**
    - `layout.tsx`: Standalone div wrapper (same pattern as /intake/[slug]/layout.tsx -- no sidebar, no auth). Minimal wrapper since root layout.tsx provides html/body/fonts/Toaster.
    - `page.tsx`: RSC page.
      - Params: `{ slug: string }` (from URL).
      - Fetch template by slug via getTemplateBySlug(slug). This gives template + branding (accentColor, logoUrl, welcomeText).
      - Join with lawyer profile: query lawyerProfiles where userId matches template.lawyerId. Get firmName, specialties, description, photoUrl.
      - If template not found: render Alert component with "Ce cabinet n'existe pas ou n'est plus disponible." per UI-SPEC error state.
      - Set CSS custom properties on container div: --lawyer-accent from template branding (same pattern as /intake/[slug]).
      - Render three sections:
        1. LawyerHero (hero section with bg-muted, 48px vertical padding)
        2. Specialty cards section ("Nos domaines d'expertise", heading h2)
        3. Intake form section ("Formulaire de prise de contact", heading h2, id="intake" for anchor scroll)
      - The intake form section renders DynamicStepper directly (reuse from Phase 8) with the template data.
      - CobrandingFooter at bottom.
    - `generateMetadata` (per D-05): Return title `"{firmName} | Consultation en ligne | LegalConnect"`, description with specialties, openGraph type "website".
    - `loading.tsx`: Skeleton with hero placeholder (Skeleton h-20 + h-8 + h-4) + 3 specialty card skeletons + stepper skeleton.

    **3. Create opengraph-image.tsx (per D-05):**
    - Use ImageResponse from "next/og".
    - Export runtime = "edge", alt, size (1200x630), contentType "image/png".
    - Fetch lawyer profile + template by slug.
    - Render: deep blue background (#1a365d or template accent color), firm name in white 48px, specialties in 24px with opacity 0.8, "Consultation en ligne | LegalConnect" in 20px with opacity 0.6.
    - If no profile found: render generic LegalConnect branded fallback.

    **4. Create LawyerHero component:**
    - Props: firmName, specialties (string[]), description (optional), photoUrl (optional), accentColor.
    - Layout: centered text, max-w-3xl mx-auto, bg-muted section with px-md py-2xl.
    - If photoUrl: Avatar component (80px circle) with lawyer photo. Alt text: "{firmName}, avocat".
    - Firm name as h1 (Display 28px, font-semibold).
    - Specialties as Badge list (one per specialty).
    - Description paragraph if provided, else default: "Bienvenue sur notre espace de consultation en ligne. Decrivez votre situation en toute confidentialite." per UI-SPEC.
    - CTA Button: "Commencer ma demande" with accent color. onClick: smooth scroll to #intake anchor.
    - The scroll handler is client-side, so extract the CTA button into a small client component or use an anchor link `<a href="#intake">` with scroll-behavior: smooth CSS.

    **5. Create SpecialtyCard component:**
    - Props: name (string), icon (optional Lucide icon name).
    - Uses shadcn Card component.
    - Map common specialty names to Lucide icons: famille -> Heart, travail -> Briefcase, penal -> Scale, immobilier -> Home, commercial -> Building, etc. Fallback: Gavel icon.
    - Display: icon + specialty name in Card.
    - Grid layout: 3-column on lg, 2-column on md, stacked on sm. Gap 16px.
  </action>
  <verify>
    <automated>cd /Users/dhamon && pnpm tsc --noEmit --project apps/web/tsconfig.json 2>&1 | tail -5 && grep -q "generateMetadata" apps/web/src/app/cabinet-\\[slug\\]/page.tsx && grep -q "ImageResponse" apps/web/src/app/cabinet-\\[slug\\]/opengraph-image.tsx && grep -q "Commencer ma demande" apps/web/src/components/landing/lawyer-hero.tsx && grep -q "description" apps/web/src/lib/db/schema/lawyer.ts && grep -q "photo_url" apps/web/src/lib/db/schema/lawyer.ts && echo "LANDING PAGE OK"</automated>
  </verify>
  <done>Hosted lawyer page at /cabinet-[slug] shows hero with firm name/photo/specialties/CTA, specialty cards grid, and reused DynamicStepper intake form. Dynamic OG meta tags and OG image generated from lawyer profile. 404 handled gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Integration settings page with snippet and preview</name>
  <files>
    apps/web/src/app/(app)/settings/cabinet/integration/page.tsx,
    apps/web/src/components/integration/snippet-copy-block.tsx,
    apps/web/src/components/integration/widget-preview.tsx
  </files>
  <read_first>
    apps/web/src/app/(app)/settings/cabinet/page.tsx,
    apps/web/src/app/(app)/settings/cabinet/cabinet-form.tsx,
    apps/web/src/app/(app)/settings/layout.tsx,
    apps/web/src/server/actions/lawyer-settings.actions.ts,
    apps/web/src/server/actions/template.actions.ts
  </read_first>
  <action>
    **1. Create /settings/cabinet/integration/page.tsx (per D-07):**
    - RSC page inside (app) group (uses existing settings layout with sidebar).
    - Fetch lawyer profile via getLawyerProfile(). Fetch template via getTemplateForLawyer().
    - If no template with slug configured: show Alert "Completez votre profil cabinet pour generer le code d'integration." with link to /settings/cabinet/template.
    - Otherwise, build the page with three sections:

    **Section 1: Widget snippet (per D-07, UI-SPEC):**
    - Title h2 "Integration" (Heading 20px semibold).
    - Subtitle: "Integrez le formulaire d'intake directement sur votre site web."
    - 3-step instructions:
      - "Etape 1 : Copiez le code" with SnippetCopyBlock below.
      - "Etape 2 : Collez-le avant </body>" with body text: "Ajoutez ce code juste avant la balise </body> de votre site."
      - "Etape 3 : C'est pret !" with body text: "Le bouton flottant apparaitra en bas a droite de votre site."
    - Steps styled with semibold label (Label 14px) + body description (Body 14px).

    **Section 2: Hosted page link (per D-07):**
    - Separator.
    - Title h2 "Page hebergee".
    - Body: "Partagez ce lien avec vos clients :"
    - Link block: readonly input showing `https://app.legalconnect.fr/cabinet-{slug}` with "Copier le lien" button. On click: clipboard API, toast "Lien copie dans le presse-papier." via sonner.
    - NOTE: Use process.env.NEXT_PUBLIC_APP_URL or window.location.origin for the base URL (not hardcoded).

    **Section 3: Widget preview:**
    - Separator.
    - Title h2 "Apercu du widget".
    - WidgetPreview component showing a static mockup of the widget appearance.

    **2. Create SnippetCopyBlock component:**
    - Props: slug (string), accentColor (string).
    - Generates the script tag string: `<script src="${appUrl}/api/widget?v=1" data-slug="${slug}" data-color="${accentColor}"></script>`
    - Display in a code block: bg-muted, font-mono, rounded-lg, p-md.
    - "Copier" ghost button with ClipboardCopy icon (from Lucide).
    - On click: navigator.clipboard.writeText(snippet). Button text changes to "Copie !" for 2 seconds. Toast "Code copie dans le presse-papier." via sonner.
    - This is a client component ("use client") for clipboard interaction.

    **3. Create WidgetPreview component:**
    - Props: accentColor (string), firmName (string).
    - Static visual preview (not interactive): bordered container (border rounded-lg), min-height 400px, relative positioned.
    - Renders a scaled-down mockup:
      - Small circle button in bottom-right (accent color background, 32px).
      - Small modal mockup (scaled down, ~200px wide) showing header with firm name, placeholder form fields, footer text.
    - All static divs with inline styles or Tailwind classes. No actual widget loaded.
    - This is a pure presentational component, can be server component.
  </action>
  <verify>
    <automated>cd /Users/dhamon && pnpm tsc --noEmit --project apps/web/tsconfig.json 2>&1 | tail -5 && grep -q "widget.js\|widget" apps/web/src/app/\\(app\\)/settings/cabinet/integration/page.tsx && grep -q "clipboard" apps/web/src/components/integration/snippet-copy-block.tsx && grep -q "data-slug" apps/web/src/components/integration/snippet-copy-block.tsx && test -f apps/web/src/components/integration/widget-preview.tsx && echo "INTEGRATION PAGE OK"</automated>
  </verify>
  <done>Integration settings page shows 3-step widget installation instructions with copyable script snippet, copyable hosted page link, and static widget preview. All copy actions use clipboard API with toast feedback.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `pnpm tsc --noEmit`
2. /cabinet-[slug] page.tsx exports generateMetadata and renders DynamicStepper
3. opengraph-image.tsx uses ImageResponse
4. /settings/cabinet/integration shows snippet with data-slug
5. Copy buttons use clipboard API
6. Non-existent slug shows 404 error state
</verification>

<success_criteria>
- /cabinet-[slug] renders landing page with lawyer hero, specialty cards, and DynamicStepper intake form
- Dynamic OG meta tags and OG image generated from lawyer profile (per D-05)
- /settings/cabinet/integration shows ready-to-copy widget snippet (per D-07)
- Hosted page link is copyable from integration settings
- 404 error state shown for non-existent slugs
- All pages TypeScript-clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution-embedding/09-03-SUMMARY.md`
</output>
