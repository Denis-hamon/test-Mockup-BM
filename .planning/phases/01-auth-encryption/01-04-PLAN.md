---
phase: 01-auth-encryption
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - apps/web/src/app/(auth)/recovery/page.tsx
  - apps/web/src/app/(auth)/recovery/restore/page.tsx
  - apps/web/src/components/auth/recovery-display.tsx
  - apps/web/src/components/auth/recovery-verify.tsx
  - apps/web/src/components/auth/recovery-restore.tsx
  - apps/web/src/server/actions/encryption.actions.ts
  - apps/web/src/server/actions/rgpd.actions.ts
  - apps/web/src/app/(app)/settings/privacy/page.tsx
  - apps/web/src/app/(app)/settings/export/page.tsx
  - apps/web/src/app/(app)/settings/delete/page.tsx
  - apps/web/src/app/(app)/settings/layout.tsx
  - apps/web/src/app/(app)/layout.tsx
  - apps/web/src/app/(app)/dashboard/page.tsx
  - apps/web/src/components/settings/consent-manager.tsx
  - apps/web/src/components/settings/export-data.tsx
  - apps/web/src/components/settings/delete-account.tsx
  - packages/email/src/deletion-confirmation.tsx
  - packages/email/src/index.ts
autonomous: true
requirements:
  - SECU-01
  - SECU-02
  - AUTH-02

must_haves:
  truths:
    - "After registration, user sees a blocking screen displaying 12-word recovery passphrase"
    - "User must verify 3 random words before proceeding"
    - "User's keypair and encrypted private key are stored in the database"
    - "User on a new device can restore their private key using the recovery passphrase"
    - "User can manage granular RGPD consent (essential vs analytics) in settings"
    - "User can request data export (ZIP with JSON + files)"
    - "User can request account deletion with 30-day grace period"
    - "Deletion confirmation email is sent"
  artifacts:
    - path: "apps/web/src/components/auth/recovery-display.tsx"
      provides: "Blocking passphrase display screen"
      contains: "mnemonic"
    - path: "apps/web/src/components/auth/recovery-verify.tsx"
      provides: "3-word verification step"
      contains: "verifyPassphraseWords"
    - path: "apps/web/src/server/actions/encryption.actions.ts"
      provides: "Store/retrieve encryption keys"
      exports: ["storeEncryptionKeys", "getEncryptionKeys"]
    - path: "apps/web/src/server/actions/rgpd.actions.ts"
      provides: "RGPD compliance actions"
      exports: ["updateConsent", "getConsents", "requestDataExport", "requestAccountDeletion", "cancelAccountDeletion"]
    - path: "apps/web/src/app/(app)/settings/privacy/page.tsx"
      provides: "RGPD consent management page"
      contains: "ConsentManager"
    - path: "apps/web/src/app/(app)/settings/delete/page.tsx"
      provides: "Account deletion page"
      contains: "DeleteAccount"
  key_links:
    - from: "apps/web/src/components/auth/recovery-display.tsx"
      to: "packages/crypto"
      via: "generateKeypair, generateRecoveryMnemonic, encryptPrivateKey"
      pattern: "generateKeypair|generateRecoveryMnemonic|encryptPrivateKey"
    - from: "apps/web/src/components/auth/recovery-verify.tsx"
      to: "packages/crypto"
      via: "selectRandomWordIndices, verifyPassphraseWords"
      pattern: "selectRandomWordIndices|verifyPassphraseWords"
    - from: "apps/web/src/server/actions/encryption.actions.ts"
      to: "apps/web/src/lib/db/schema/encryption.ts"
      via: "encryptionKeys table insert/select"
      pattern: "encryptionKeys"
    - from: "apps/web/src/server/actions/rgpd.actions.ts"
      to: "apps/web/src/lib/db/schema/consent.ts"
      via: "consents table insert/update"
      pattern: "consents"
---

<objective>
Wire E2E encryption into the registration flow (passphrase display + verification) and implement RGPD compliance infrastructure (consent management, data export, account deletion with 30-day grace period).

Purpose: This completes Phase 1 by connecting the crypto primitives (plan 03) with the auth system (plan 02) and adding the legally required RGPD features. After this plan, users have full E2E encryption key management and can exercise their data rights.
Output: Recovery passphrase flow post-registration, key restoration on new devices, consent settings page, data export, and soft-delete account deletion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-encryption/01-CONTEXT.md
@.planning/phases/01-auth-encryption/01-RESEARCH.md
@.planning/phases/01-auth-encryption/01-02-SUMMARY.md
@.planning/phases/01-auth-encryption/01-03-SUMMARY.md

<interfaces>
<!-- From plan 01-02: auth system -->
From apps/web/src/lib/auth.ts:
- export { handlers, auth, signIn, signOut } (Auth.js v5 config)
- JWT session contains: user.id, user.email, user.role

From apps/web/src/server/actions/auth.actions.ts:
- export async function registerUser(formData: FormData): Promise<{ success: true } | { error: string }>
- export async function verifyEmail(token: string): Promise<{ success: true } | { error: string }>

<!-- From plan 01-03: crypto primitives -->
From packages/crypto/src/index.ts:
- export { generateKeypair, publicKeyToBase64, base64ToKey } from keypair
- export { encrypt, decrypt } from encrypt
- export { deriveKey, generateSalt } from kdf
- export { generateRecoveryMnemonic, encryptPrivateKey, decryptPrivateKey, selectRandomWordIndices, verifyPassphraseWords } from recovery
- export type { KeyPair, EncryptedData, RecoveryBundle } from types

<!-- From plan 01-01: DB schemas -->
From apps/web/src/lib/db/schema/encryption.ts:
- export const encryptionKeys (id, userId unique, publicKey, encryptedPrivateKey, recoverySalt, recoveryNonce, recoveryParams, createdAt, updatedAt)

From apps/web/src/lib/db/schema/consent.ts:
- export const consents (id, userId, type enum ["essential","analytics"], granted, grantedAt, revokedAt, ipAddress, userAgent)

From apps/web/src/lib/db/schema/auth.ts:
- users table has deletedAt, deletionScheduledAt columns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recovery passphrase flow (display, verify, store keys) and key restoration</name>
  <files>
    apps/web/src/app/(auth)/recovery/page.tsx,
    apps/web/src/app/(auth)/recovery/restore/page.tsx,
    apps/web/src/components/auth/recovery-display.tsx,
    apps/web/src/components/auth/recovery-verify.tsx,
    apps/web/src/components/auth/recovery-restore.tsx,
    apps/web/src/server/actions/encryption.actions.ts,
    apps/web/src/app/(app)/layout.tsx,
    apps/web/src/app/(app)/dashboard/page.tsx
  </files>
  <read_first>
    /Users/dhamon/.planning/phases/01-auth-encryption/01-CONTEXT.md
    /Users/dhamon/.planning/phases/01-auth-encryption/01-RESEARCH.md
    /Users/dhamon/packages/crypto/src/index.ts
    /Users/dhamon/packages/crypto/src/recovery.ts
    /Users/dhamon/packages/crypto/src/keypair.ts
    /Users/dhamon/apps/web/src/lib/db/schema/encryption.ts
    /Users/dhamon/apps/web/src/server/actions/auth.actions.ts
    /Users/dhamon/apps/web/src/lib/auth.ts
  </read_first>
  <action>
    **Encryption server actions** (`apps/web/src/server/actions/encryption.actions.ts`):
    ```typescript
    "use server";
    import { db } from "@/lib/db";
    import { encryptionKeys } from "@/lib/db/schema/encryption";
    import { auth } from "@/lib/auth";
    import { eq } from "drizzle-orm";

    export async function storeEncryptionKeys(data: {
      publicKey: string;        // base64
      encryptedPrivateKey: string; // base64
      recoverySalt: string;     // base64
      recoveryNonce: string;    // base64
      recoveryParams: string;   // JSON string: { opslimit, memlimit, alg }
    }) {
      const session = await auth();
      if (!session?.user?.id) return { error: "Non authentifie" };
      // Upsert: insert or update if exists
      const existing = await db.query.encryptionKeys.findFirst({
        where: eq(encryptionKeys.userId, session.user.id),
      });
      if (existing) {
        await db.update(encryptionKeys)
          .set({ ...data, updatedAt: new Date() })
          .where(eq(encryptionKeys.userId, session.user.id));
      } else {
        await db.insert(encryptionKeys).values({
          userId: session.user.id,
          ...data,
        });
      }
      return { success: true };
    }

    export async function getEncryptionKeys() {
      const session = await auth();
      if (!session?.user?.id) return { error: "Non authentifie" };
      const keys = await db.query.encryptionKeys.findFirst({
        where: eq(encryptionKeys.userId, session.user.id),
      });
      if (!keys) return { error: "Aucune cle trouvee" };
      return {
        publicKey: keys.publicKey,
        encryptedPrivateKey: keys.encryptedPrivateKey,
        recoverySalt: keys.recoverySalt,
        recoveryNonce: keys.recoveryNonce,
        recoveryParams: keys.recoveryParams,
      };
    }
    ```

    **Recovery display page** (`apps/web/src/app/(auth)/recovery/page.tsx`):
    Server component. Checks session (user must be authenticated after registration). Renders RecoveryDisplay then RecoveryVerify as a two-step client flow.

    **RecoveryDisplay** (`apps/web/src/components/auth/recovery-display.tsx`):
    "use client". Per D-01 (system-generated 12 words), D-02 (loss = permanent), D-03 (blocking screen):

    On mount:
    1. Call `generateKeypair()` from @legalconnect/crypto
    2. Call `generateRecoveryMnemonic()` from @legalconnect/crypto
    3. Store mnemonic in component state (NEVER sent to server)
    4. Call `encryptPrivateKey(keypair.secretKey, mnemonic)` to get RecoveryBundle
    5. Call `storeEncryptionKeys()` server action with publicKey (base64), bundle fields
    6. Store secretKey in IndexedDB for current session use (key: "legalconnect_private_key")

    Display:
    - Warning banner (red/amber): "IMPORTANT : Notez ces 12 mots dans l'ordre exact. Ils sont votre unique moyen de recuperer vos donnees chiffrees. En cas de perte, vos donnees seront definitivement inaccessibles." (D-02)
    - 12 words displayed in a 3x4 grid, each word numbered 1-12
    - "Copier" button to copy all 12 words to clipboard
    - "J'ai note ma phrase de recuperation" button (primary) to proceed to verification
    - NO skip button, NO close button (D-03 blocking)

    **RecoveryVerify** (`apps/web/src/components/auth/recovery-verify.tsx`):
    "use client". Per D-04 (verify 3 random words):

    Props: mnemonic (string), onVerified (() => void)
    1. On mount, call `selectRandomWordIndices(3)` to get 3 random positions
    2. Display 3 input fields: "Mot n.{idx+1}" for each selected index
    3. Submit button "Verifier"
    4. On submit, call `verifyPassphraseWords(mnemonic, attempts)` from @legalconnect/crypto
    5. If verified: call onVerified, redirect to /dashboard
    6. If failed: show error "Les mots ne correspondent pas. Veuillez reessayer." and allow retry

    **Recovery restore page** (`apps/web/src/app/(auth)/recovery/restore/page.tsx`):
    For users logging in on a new device who need to recover their private key.
    Renders RecoveryRestore client component.

    **RecoveryRestore** (`apps/web/src/components/auth/recovery-restore.tsx`):
    "use client". Shown when user logs in but has no private key in IndexedDB.
    1. Call `getEncryptionKeys()` server action to get the encrypted bundle
    2. Display 12 input fields for mnemonic entry (or single textarea)
    3. On submit: reconstruct mnemonic string, call `decryptPrivateKey(bundle, mnemonic)`
    4. If success: store secretKey in IndexedDB, redirect to /dashboard
    5. If failure: show error "Phrase de recuperation incorrecte. Vos donnees ne peuvent pas etre dechiffrees." (D-02: no server recourse)

    **App layout** (`apps/web/src/app/(app)/layout.tsx`):
    Basic authenticated layout with a sidebar placeholder and header with user info. Must check session via `auth()` and redirect to /login if not authenticated.

    **Dashboard placeholder** (`apps/web/src/app/(app)/dashboard/page.tsx`):
    Simple placeholder: "Bienvenue sur LegalConnect" with user role displayed. Will be replaced in Phase 6.

    **Update proxy.ts**: Add `/recovery` to the list of auth pages that don't require redirection to /dashboard if logged in. Users must be able to access /recovery after registration.
  </action>
  <verify>
    <automated>cd /Users/dhamon && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/server/actions/encryption.actions.ts contains "use server" and "storeEncryptionKeys" and "getEncryptionKeys" and "encryptionKeys"
    - apps/web/src/components/auth/recovery-display.tsx contains "use client" and "generateKeypair" and "generateRecoveryMnemonic" and "encryptPrivateKey" and "storeEncryptionKeys"
    - apps/web/src/components/auth/recovery-display.tsx contains "definitivement inaccessibles" (D-02 warning)
    - apps/web/src/components/auth/recovery-display.tsx does NOT contain any "skip" or "passer" button (D-03 blocking)
    - apps/web/src/components/auth/recovery-verify.tsx contains "selectRandomWordIndices" and "verifyPassphraseWords" (D-04)
    - apps/web/src/components/auth/recovery-restore.tsx contains "getEncryptionKeys" and "decryptPrivateKey"
    - apps/web/src/app/(app)/layout.tsx contains "auth()" session check
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Recovery passphrase flow works: after registration, user sees 12 words (D-01), warning about permanent loss (D-02), blocking screen (D-03), verifies 3 words (D-04). Keys stored in DB. New device recovery via passphrase entry.</done>
</task>

<task type="auto">
  <name>Task 2: RGPD compliance — consent management, data export, account deletion with 30-day grace</name>
  <files>
    apps/web/src/server/actions/rgpd.actions.ts,
    apps/web/src/app/(app)/settings/layout.tsx,
    apps/web/src/app/(app)/settings/privacy/page.tsx,
    apps/web/src/app/(app)/settings/export/page.tsx,
    apps/web/src/app/(app)/settings/delete/page.tsx,
    apps/web/src/components/settings/consent-manager.tsx,
    apps/web/src/components/settings/export-data.tsx,
    apps/web/src/components/settings/delete-account.tsx,
    packages/email/src/deletion-confirmation.tsx,
    packages/email/src/index.ts
  </files>
  <read_first>
    /Users/dhamon/.planning/phases/01-auth-encryption/01-CONTEXT.md
    /Users/dhamon/apps/web/src/lib/db/schema/consent.ts
    /Users/dhamon/apps/web/src/lib/db/schema/auth.ts
    /Users/dhamon/apps/web/src/lib/auth.ts
    /Users/dhamon/packages/email/src/index.ts
    /Users/dhamon/packages/shared/src/schemas/consent.ts
  </read_first>
  <action>
    **RGPD server actions** (`apps/web/src/server/actions/rgpd.actions.ts`):
    ```typescript
    "use server";
    ```
    Per D-09 (granular consent), D-10 (ZIP export), D-11 (soft delete 30 days):

    - `getConsents()`: Get current user's consents from DB. Return array of `{ type, granted, grantedAt, revokedAt }`.

    - `updateConsent(data: { type: "essential" | "analytics", granted: boolean })`: Per D-09. Validate with consentUpdateSchema from @legalconnect/shared. If granting: insert new consent record with grantedAt=now, ipAddress from headers, userAgent from headers. If revoking: update existing consent to set revokedAt=now. Essential consent cannot be revoked (always required). Return updated consents.

    - `requestDataExport()`: Per D-10. Collect all user data: profile, consents, encryption keys metadata (NOT private keys). Package as JSON. In v1, return JSON directly as download (ZIP with files deferred to when file uploads exist). Return `{ data: string }` (JSON stringified).

    - `requestAccountDeletion()`: Per D-11. Set `deletedAt = now()` and `deletionScheduledAt = now + 30 days` on users table. Invalidate all sessions (delete from sessions table where userId matches). Send deletion confirmation email (D-12 tone). Return `{ success: true, scheduledPurgeDate: string }`.

    - `cancelAccountDeletion()`: Per D-11 (annulation possible pendant le delai). Clear `deletedAt` and `deletionScheduledAt` on users table. Return `{ success: true }`.

    **Settings layout** (`apps/web/src/app/(app)/settings/layout.tsx`):
    Sidebar nav with links: "Confidentialite" (/settings/privacy), "Exporter mes donnees" (/settings/export), "Supprimer mon compte" (/settings/delete). Uses shadcn nav components.

    **Privacy page** (`apps/web/src/app/(app)/settings/privacy/page.tsx`):
    Server component. Fetches consents via getConsents(). Renders ConsentManager.

    **ConsentManager** (`apps/web/src/components/settings/consent-manager.tsx`):
    "use client". Per D-09 (granular: essential vs analytics):
    - "Donnees essentielles" toggle — always ON, disabled (cannot revoke essential)
    - "Analytics et amelioration" toggle — user can toggle on/off
    - Each toggle calls updateConsent server action
    - Show last update date for each consent
    Uses shadcn Switch component.

    **Export page** (`apps/web/src/app/(app)/settings/export/page.tsx`):
    Server component. Renders ExportData.

    **ExportData** (`apps/web/src/components/settings/export-data.tsx`):
    "use client". Per D-10:
    - Button "Exporter mes donnees"
    - On click: call requestDataExport(), trigger browser download of JSON file
    - Show loading state during export
    - Note: "Vos donnees vous seront fournies au format JSON."

    **Delete page** (`apps/web/src/app/(app)/settings/delete/page.tsx`):
    Server component. Renders DeleteAccount.

    **DeleteAccount** (`apps/web/src/components/settings/delete-account.tsx`):
    "use client". Per D-11 (soft delete, 30-day grace):
    - Warning text: "La suppression de votre compte est irreversible apres 30 jours. Pendant cette periode, vous pouvez annuler la suppression."
    - Confirm checkbox: "Je comprends que mes donnees chiffrees seront definitivement supprimees"
    - Button "Supprimer mon compte" (destructive variant, red)
    - On click: call requestAccountDeletion(), show confirmation with scheduled purge date
    - If account already scheduled for deletion: show "Suppression prevue le {date}" with "Annuler la suppression" button calling cancelAccountDeletion()

    **Deletion email** (`packages/email/src/deletion-confirmation.tsx`):
    React Email component. Subject: "Confirmation de suppression de compte - LegalConnect".
    Body (D-12 warm tone, vouvoiement): "Votre demande de suppression de compte a ete prise en compte. Votre compte sera definitivement supprime le {date}. Si vous changez d'avis, connectez-vous avant cette date pour annuler la suppression."

    **Update packages/email/src/index.ts**: Add `sendDeletionConfirmationEmail(to, scheduledDate)` export.
  </action>
  <verify>
    <automated>cd /Users/dhamon && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/server/actions/rgpd.actions.ts contains "use server" and "getConsents" and "updateConsent" and "requestDataExport" and "requestAccountDeletion" and "cancelAccountDeletion"
    - apps/web/src/server/actions/rgpd.actions.ts contains "deletionScheduledAt" and "30" (30-day grace per D-11)
    - apps/web/src/server/actions/rgpd.actions.ts contains "essential" check (essential consent cannot be revoked per D-09)
    - apps/web/src/components/settings/consent-manager.tsx contains "use client" and "essential" and "analytics" and "updateConsent" (D-09)
    - apps/web/src/components/settings/export-data.tsx contains "requestDataExport" and "JSON" (D-10)
    - apps/web/src/components/settings/delete-account.tsx contains "requestAccountDeletion" and "cancelAccountDeletion" and "30 jours" (D-11)
    - packages/email/src/deletion-confirmation.tsx contains "suppression" and "LegalConnect"
    - packages/email/src/index.ts contains "sendDeletionConfirmationEmail"
    - apps/web/src/app/(app)/settings/privacy/page.tsx exists
    - apps/web/src/app/(app)/settings/export/page.tsx exists
    - apps/web/src/app/(app)/settings/delete/page.tsx exists
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>RGPD compliance complete: granular consent management (D-09), data export as JSON (D-10), soft-delete account deletion with 30-day grace period and cancellation (D-11), deletion confirmation email sent.</done>
</task>

</tasks>

<verification>
- `pnpm build` exits 0
- Recovery passphrase flow: display 12 words, verify 3 random words, store keys
- Key restoration: enter 12 words, decrypt private key, store in IndexedDB
- RGPD consent: essential (locked on) + analytics (toggleable)
- Data export: generates JSON download
- Account deletion: sets 30-day purge, sends email, allows cancellation
</verification>

<success_criteria>
- Registration redirects to /recovery showing 12 words (D-01, D-03)
- Warning about permanent loss displayed prominently (D-02)
- User must verify 3 random words before proceeding (D-04)
- Encryption keys (public + encrypted private) stored in database
- New device recovery works via 12-word passphrase entry
- Settings/privacy page shows granular consent toggles (D-09)
- Settings/export generates downloadable JSON (D-10)
- Settings/delete triggers soft delete with 30-day grace period (D-11)
- Deletion confirmation email sent with warm French tone (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-encryption/01-04-SUMMARY.md`
</output>
