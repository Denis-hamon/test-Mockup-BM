---
phase: 07-client-portal
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/src/app/(app)/layout.tsx
  - apps/web/src/components/portal/client-sidebar.tsx
  - apps/web/src/components/portal/dashboard-summary.tsx
  - apps/web/src/components/portal/case-list-client.tsx
  - apps/web/src/components/portal/case-status-tracker.tsx
  - apps/web/src/components/portal/client-timeline.tsx
  - apps/web/src/components/portal/document-grid.tsx
  - apps/web/src/components/portal/security-header.tsx
  - apps/web/src/app/(app)/portail/page.tsx
  - apps/web/src/app/(app)/portail/dossiers/page.tsx
  - apps/web/src/app/(app)/portail/dossiers/[id]/page.tsx
  - apps/web/src/app/(app)/portail/documents/page.tsx
  - apps/web/src/app/(app)/portail/parametres/page.tsx
autonomous: true
requirements: [PORT-02, PORT-03]

must_haves:
  truths:
    - "Client sees a sidebar with role-appropriate navigation (Accueil, Mes dossiers, Messages, RDV, Documents, Parametres)"
    - "Client dashboard shows active cases count, unread messages count, and next appointment"
    - "Client can view case list with status badges and navigate to case detail"
    - "Client can see case status tracker (Nouveau > En cours > Termine) and simplified timeline"
    - "Client can browse and download uploaded documents"
  artifacts:
    - path: "apps/web/src/app/(app)/layout.tsx"
      provides: "Role-conditional sidebar layout"
      contains: "ClientSidebar"
    - path: "apps/web/src/app/(app)/portail/page.tsx"
      provides: "Client dashboard home"
      contains: "DashboardSummary"
    - path: "apps/web/src/components/portal/client-sidebar.tsx"
      provides: "Client navigation sidebar"
      contains: "usePathname"
  key_links:
    - from: "apps/web/src/app/(app)/portail/page.tsx"
      to: "apps/web/src/server/actions/portal.actions.ts"
      via: "getClientDashboard server action"
      pattern: "getClientDashboard"
    - from: "apps/web/src/app/(app)/portail/dossiers/page.tsx"
      to: "apps/web/src/server/actions/portal.actions.ts"
      via: "getClientCases server action"
      pattern: "getClientCases"
---

<objective>
Portal shell UI: role-conditional layout with sidebar, all non-messaging portal pages (dashboard, cases, case detail, documents, settings).

Purpose: Give clients a secure, trust-inspiring navigation shell and pages to track cases, browse documents, and manage settings. Chat UI is split into Plan 03 to keep this plan under context budget.
Output: Modified app layout + 5 portal pages + 7 portal UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-client-portal/07-CONTEXT.md
@.planning/phases/07-client-portal/07-RESEARCH.md
@.planning/phases/07-client-portal/07-UI-SPEC.md
@.planning/phases/07-client-portal/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From apps/web/src/server/actions/portal.actions.ts:
```typescript
export async function requireClient(): Promise<AuthResult>;
export async function requireAuth(): Promise<AuthResult>;
export async function getClientDashboard(): Promise<{activeCases: number; unreadMessages: number; nextAppointment: Date | null; recentActivity: Array<{type: string; description: string; date: Date; caseId: string}>}>;
export async function getClientCases(): Promise<ClientCase[]>;
export async function getClientCaseDetail(caseId: string): Promise<ClientCaseDetail>;
export async function getClientDocuments(): Promise<ClientDocument[]>;
```

From apps/web/src/server/actions/messaging.actions.ts:
```typescript
export async function getUnreadCount(): Promise<number>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout, sidebar, and portal shell components</name>
  <files>
    apps/web/src/app/(app)/layout.tsx,
    apps/web/src/components/portal/client-sidebar.tsx,
    apps/web/src/components/portal/security-header.tsx,
    apps/web/src/components/portal/dashboard-summary.tsx,
    apps/web/src/components/portal/case-list-client.tsx,
    apps/web/src/components/portal/case-status-tracker.tsx,
    apps/web/src/components/portal/client-timeline.tsx,
    apps/web/src/components/portal/document-grid.tsx
  </files>
  <read_first>
    apps/web/src/app/(app)/layout.tsx,
    apps/web/src/components/dashboard/case-detail-page.tsx,
    apps/web/src/components/dashboard/case-data-table.tsx,
    apps/web/src/components/dashboard/status-badge.tsx,
    apps/web/src/components/upload/file-preview.tsx,
    apps/web/src/server/actions/portal.actions.ts,
    .planning/phases/07-client-portal/07-UI-SPEC.md
  </read_first>
  <action>
    **Install shadcn components first:**
    ```
    cd apps/web && pnpm dlx shadcn@latest add scroll-area avatar form collapsible
    ```

    **1. Modify `apps/web/src/app/(app)/layout.tsx`** (per D-13):

    Replace the current flat header layout with a sidebar-based layout. Read the session and check `session.user.role`:
    - If `role === "avocat"`: render existing header links (Dossiers, Parametres) in a sidebar (or keep existing nav for now, since lawyer dashboard was built with header nav). Actually per D-13: "Layout partage avec le dashboard avocat + theme different. Meme structure (sidebar, header) mais contenu de la sidebar adapte au role." So BOTH roles get sidebar.
    - If `role === "client"`: render `<ClientSidebar />`
    - Sidebar: `hidden lg:block lg:w-60` with `border-r bg-card`. On mobile: Sheet (hamburger).
    - Header: add `<SecurityHeader />` badge per D-14
    - Main content: `flex-1 px-6 py-8`

    Keep backward compatibility: existing `/dashboard` and `/dossiers` routes still work for avocat role.

    **2. Create `apps/web/src/components/portal/security-header.tsx`** (per D-14):

    Renders `<ShieldCheck className="h-4 w-4" />` + "Connexion securisee" in trust-green color (`hsl(142 71% 45%)`). Use Badge component variant outline with custom green styling. sr-only expanded text.

    **3. Create `apps/web/src/components/portal/client-sidebar.tsx`** (per D-12, UI-SPEC sidebar nav):

    Client component. Navigation items with Lucide icons:
    - Home -> /portail (Accueil)
    - FolderOpen -> /portail/dossiers (Mes dossiers) + active case count badge
    - MessageSquare -> /portail/messages (Messages) + unread count badge
    - CalendarDays -> /portail/rendez-vous (Rendez-vous) + pending count badge
    - FileText -> /portail/documents (Documents)
    - Settings -> /portail/parametres (Parametres)

    Active item: primary bg + primary-foreground text. Use `usePathname()` to detect active. Unread badges: call `getUnreadCount()` from messaging.actions, pass as prop from layout or fetch client-side.

    Mobile: render inside a Sheet component with `aria-label="Menu de navigation"`. 44px min touch targets.

    **4. Create `apps/web/src/components/portal/dashboard-summary.tsx`** (per D-12, UI-SPEC):

    Client component. Three summary cards in 3-col grid (lg), 1-col (mobile):
    - "Dossiers actifs" with FolderOpen icon + count. Click navigates to /portail/dossiers.
    - "Messages non lus" with MessageSquare icon + count. Click navigates to /portail/messages.
    - "Prochain rendez-vous" with CalendarDays icon + date or "Aucun rendez-vous prevu". Click navigates to /portail/rendez-vous.

    Below: "Activite recente" section with last 5 events as a simplified timeline list.

    **5. Create `apps/web/src/components/portal/case-list-client.tsx`** (per D-06, D-07):

    Card-based list. Each card shows:
    - Status badge (Nouveau/En cours/Termine per UI-SPEC status badge colors)
    - Problem type (bold)
    - "Soumis le [date]" formatted with date-fns French locale
    - Last message relative time + unread count if > 0 (blue dot + count)
    - Click navigates to `/portail/dossiers/[id]`

    Sort: most recent activity first.

    **6. Create `apps/web/src/components/portal/case-status-tracker.tsx`** (per D-06, UI-SPEC):

    Horizontal progress indicator: 3 steps (Nouveau, En cours, Termine). Current step filled with primary color. Steps connected by lines. Use `role="progressbar"` with `aria-valuenow` (1/2/3) and `aria-valuetext`.

    **7. Create `apps/web/src/components/portal/client-timeline.tsx`**:

    Simplified timeline of events: submission date, status changes, message exchanges. Each entry: circle + line + description + date. Max items configurable. Muted styling.

    **8. Create `apps/web/src/components/portal/document-grid.tsx`** (per D-08):

    Grid of documents. Reuse file-preview component. Each item: file icon, filename, size, download button. Download triggers presigned URL (or mark as disabled if not yet implemented, per Phase 6 decision).

    All components must follow UI-SPEC: Geist font, 14px body / 20px heading, spacing tokens (sm=8 md=16 lg=24), semantic colors, gap-* not space-*, cn() for conditional classes.
  </action>
  <verify>
    <automated>cd /Users/dhamon && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Layout shows role-conditional sidebar. All 7 portal shell components render. Security header badge visible. Case list shows cards with status badges. Case status tracker shows 3 steps. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Portal pages wiring (dashboard, cases, documents, settings)</name>
  <files>
    apps/web/src/app/(app)/portail/page.tsx,
    apps/web/src/app/(app)/portail/dossiers/page.tsx,
    apps/web/src/app/(app)/portail/dossiers/[id]/page.tsx,
    apps/web/src/app/(app)/portail/documents/page.tsx,
    apps/web/src/app/(app)/portail/parametres/page.tsx
  </files>
  <read_first>
    apps/web/src/server/actions/portal.actions.ts,
    apps/web/src/components/portal/dashboard-summary.tsx,
    apps/web/src/components/portal/case-list-client.tsx,
    apps/web/src/components/portal/case-status-tracker.tsx,
    apps/web/src/components/portal/document-grid.tsx,
    .planning/phases/07-client-portal/07-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `apps/web/src/app/(app)/portail/page.tsx`** (dashboard):

    RSC page. Call `getClientDashboard()`. Render "Bienvenue, [Prenom]" heading (20px semibold per UI-SPEC). Pass data to `<DashboardSummary />`. Empty state if no cases: "Vous n'avez pas encore de dossier" with subtitle.

    **2. Create `apps/web/src/app/(app)/portail/dossiers/page.tsx`**:

    RSC page. Call `getClientCases()`. Render heading "Mes dossiers" + `<CaseListClient cases={cases} />`. Empty state per UI-SPEC.

    **3. Create `apps/web/src/app/(app)/portail/dossiers/[id]/page.tsx`**:

    RSC page. Call `getClientCaseDetail(id)`. Header: back link "Retour a mes dossiers", case title, status badge, CaseStatusTracker. 3 tabs: Suivi (ClientTimeline), Documents (DocumentGrid), Messages (placeholder link to /portail/messages?conversation=ID — actual ChatContainer comes in Plan 03). Tab switch via URL hash, no page reload.

    **4. Create `apps/web/src/app/(app)/portail/documents/page.tsx`**:

    RSC page. Call `getClientDocuments()`. Render heading "Documents" + DocumentGrid. Empty state per UI-SPEC.

    **5. Create `apps/web/src/app/(app)/portail/parametres/page.tsx`** (per UI-SPEC settings):

    RSC page. Cards: "Informations personnelles" (name, email readonly), "Notifications" (toggle switches), "Securite" (recovery phrase, export). "Enregistrer les modifications" primary button. Max-w-2xl centered.

    Use existing patterns: Switch for toggles, Button for actions, Card for sections.
  </action>
  <verify>
    <automated>cd /Users/dhamon && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    All 5 portal pages render (dashboard, cases list, case detail, documents, settings). Dashboard shows "Bienvenue" + summary cards. Case detail has 3 tabs with status tracker. Empty states work. TypeScript compiles.
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` — zero errors
- All 5 portal pages exist under `apps/web/src/app/(app)/portail/`
- Layout conditionally renders sidebar based on role
- All UI follows UI-SPEC typography (14px body, 20px heading, 600 semibold)
</verification>

<success_criteria>
- Client portal shell has 5 functioning pages with role-conditional sidebar
- Dashboard shows case count, unread messages, next appointment
- Case list and detail with status tracker and simplified timeline
- Documents grid renders uploaded files
- Settings page with notification toggles
- Security header badge visible
- Mobile responsive: Sheet sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/07-client-portal/07-02-SUMMARY.md`
</output>
