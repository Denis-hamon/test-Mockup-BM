{
  "name": "legalconnect",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "db:push": "pnpm --filter web db:push",
    "db:generate": "pnpm --filter web db:generate"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
